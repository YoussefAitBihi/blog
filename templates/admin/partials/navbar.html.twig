<!--Navbar-->
<nav class="navbar navbar-expand-lg navbar-dark primary-color">
   <!-- Navbar brand -->
   <a class="navbar-brand" href="{{ path('homepage') }}">TubeVideo</a>
   <!-- Collapse button -->
   <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#basicExampleNav"
   aria-controls="basicExampleNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
   </button>
   <!-- Collapsible content -->
   <div class="collapse navbar-collapse" id="basicExampleNav">
      <!-- Links -->
      <ul class="navbar-nav mr-2 mr-sm-0">
         <li class="nav-item mr-2">
            <a class="nav-link" href="{{ path('admin_dashboard') }}">
               <i class="fas fa-home mr-1"></i>
               Accueil
               <span class="sr-only">(current)</span>
            </a>
         </li>
         <li class="nav-item mr-2">
            <a class="nav-link" href="{{ path('admin_users_index') }}">
               <i class="fas fa-user mr-1"></i>
               Utilisateurs
            </a>
         </li>
         <li class="nav-item mr-2">
            <a class="nav-link" href="{{ path('admin_posts_index') }}">
               <i class="fas fa-video mr-1"></i>
               Postes
            </a>
         </li>
         <li class="nav-item mr-2">
            <a class="nav-link" href="{{ path('admin_comments_index') }}">
               <i class="fas fa-comment mr-1"></i>
               Commentaires
            </a>
         </li>
      </ul>
      <ul class="navbar-nav ml-auto nav-flex-icons">
         <li class="nav-item dropdown avatar">
            <a class="nav-link dropdown-toggle p-0" id="navbarDropdownMenuLink-333" data-toggle="dropdown"
               aria-haspopup="true" aria-expanded="false">
               <img src="/uploads/picture/{{ app.user.picture }}" alt="Picture de {{app.user.fullName}}" class="rounded-circle z-depth-0" height="35">
            </a>
            <div class="dropdown-menu dropdown-menu-right dropdown-default"
            aria-labelledby="navbarDropdownMenuLink-333">
               <a class="dropdown-item" href="{{ path('post_new') }}">Nouvelle poste</a>
               <a class="dropdown-item" href="{{ path('user_show', {'id': app.user.id, 'slug': app.user.slug}) }}">Mon compte</a>
               <a href="{{ path('account_edit', {'id': app.user.id, 'slug': app.user.slug}) }}" class="dropdown-item">Modifier</a>
               <a class="dropdown-item" href="{{ path('account_logout') }}">Se déconnecter</a>
            </div>
         </li>
      </ul>
   </div>
   <!-- Collapsible content -->
</nav>
<!--/.Navbar-->